# Editors
.DS_Store
.idea/
.vscode/

# Node (the add-on web UI's Svelte build)
node_modules/
# The web UI is built into the image from `addon/web/src`, so the committed
# output directory is empty.
addon/rootfs/app/web/

# Python
__pycache__/
*.pyc
.venv/
.venv311/
.venv314/

# The vendor's streaming library is fetched at build time and is not ours to
# redistribute -- see the licence note in the README.
libs/
*.so
*.dylib
*.dll
