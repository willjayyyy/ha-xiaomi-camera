[pytest]
testpaths = tests
addopts = -q
# The guard tests drive a real aiohttp server; without this every one of
# them is silently skipped rather than reported as unrun.
asyncio_mode = auto
filterwarnings =
    error
