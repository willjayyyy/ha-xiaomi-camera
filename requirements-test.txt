# Pins a Home Assistant release through the plugin's patch version: 0.13.348
# tracks homeassistant 2026.7.4. Bump both together -- testing against a
# different core than the add-on is deployed against proves less than it
# appears to.
pytest-homeassistant-custom-component==0.13.348

# homeassistant.components.camera declares this in its own manifest, but the
# plugin does not install component requirements -- only camera.py pulls it
# in, at platform-import time, so nothing surfaced this until entities were
# actually set up under test.
PyTurboJPEG==1.8.3

# Read by bridge.restream when it renders the go2rtc configuration. Not pulled
# in by the plugin, and the add-on tests need it.
pyyaml

# The bridge packages the published stream with PyAV, so bridge.mux imports it
# and every module that touches a session pulls it in transitively. It reaches
# the image through the vendor SDK, whose decoder is built on it -- but CI
# deliberately never installs that SDK, so without this line the whole session
# and muxer suite fails to collect. A developer machine hides this: installing
# the SDK there brings PyAV with it.
av>=14
